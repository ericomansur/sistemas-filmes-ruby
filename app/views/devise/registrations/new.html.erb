<div class="min-h-screen flex items-center justify-center -mt-8 py-12">
  <div class="max-w-md w-full">
    <!-- Logo/Title -->
    <div class="text-center mb-8">
      <h1 class="text-5xl font-bold mb-2 bg-gradient-to-r from-indigo-400 to-purple-400 bg-clip-text text-transparent">
        🎬 CineMatch
      </h1>
      <p class="text-gray-400 text-lg">Crie sua conta gratuitamente</p>
    </div>

    <!-- Registration Form -->
    <div class="bg-gray-800 rounded-lg p-8 border border-gray-700 shadow-xl">
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "space-y-6" }) do |f| %>
        
        <!-- Error Messages -->
        <%= render "devise/shared/error_messages", resource: resource %>

        <!-- Email -->
        <div>
          <%= f.label :email, "E-mail", class: "block text-white font-medium mb-2" %>
          <%= f.email_field :email, 
                           autofocus: true, 
                           autocomplete: "email",
                           placeholder: "seu@email.com",
                           class: "w-full bg-gray-700 border border-gray-600 text-white rounded-lg px-4 py-3 focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition" %>
          <p class="text-gray-500 text-xs mt-1">Usaremos este e-mail para fazer login</p>
        </div>

        <!-- Password -->
        <div>
          <%= f.label :password, "Senha", class: "block text-white font-medium mb-2" %>
          <%= f.password_field :password, 
                              autocomplete: "new-password",
                              placeholder: "Mínimo de 6 caracteres",
                              class: "w-full bg-gray-700 border border-gray-600 text-white rounded-lg px-4 py-3 focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition" %>
          <% if @minimum_password_length %>
            <p class="text-gray-500 text-xs mt-1">
              Mínimo de <%= @minimum_password_length %> caracteres
            </p>
          <% end %>
        </div>

        <!-- Password Confirmation -->
        <div>
          <%= f.label :password_confirmation, "Confirme a Senha", class: "block text-white font-medium mb-2" %>
          <%= f.password_field :password_confirmation, 
                              autocomplete: "new-password",
                              placeholder: "Digite a senha novamente",
                              class: "w-full bg-gray-700 border border-gray-600 text-white rounded-lg px-4 py-3 focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition" %>
        </div>

        <!-- Submit Button -->
        <div>
          <%= f.submit "Criar Conta", class: "w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 px-4 rounded-lg transition cursor-pointer transform hover:scale-105" %>
        </div>
      <% end %>

      <!-- Links -->
      <div class="mt-6 text-center">
        <div class="text-gray-400 text-sm">
          Já tem uma conta?
          <%= link_to "Faça login", new_session_path(resource_name), class: "text-indigo-400 hover:text-indigo-300 font-semibold transition" %>
        </div>
      </div>
    </div>

    <!-- Back to Home -->
    <div class="text-center mt-6">
      <%= link_to root_path, class: "text-gray-400 hover:text-white text-sm transition inline-flex items-center" do %>
        <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
        </svg>
        Voltar para home
      <% end %>
    </div>

    <!-- Features Preview -->
    <div class="mt-8 grid grid-cols-3 gap-4 text-center">
      <div>
        <div class="text-3xl mb-1">🎯</div>
        <p class="text-gray-400 text-xs">Personalizado</p>
      </div>
      <div>
        <div class="text-3xl mb-1">⭐</div>
        <p class="text-gray-400 text-xs">Alta Qualidade</p>
      </div>
      <div>
        <div class="text-3xl mb-1">🚀</div>
        <p class="text-gray-400 text-xs">Rápido</p>
      </div>
    </div>
  </div>
</div>